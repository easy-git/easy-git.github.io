# 创建远程仓库

> 创建远程仓库, 目前仅支持gitee和github.

easy-git, 【命令面板】, 找到`创建远程仓库`；如果`github`或`gitee`已授权，直接输入仓库名进行创建即可。

如果没授权`github`或`gitee`，则需要先进行授权。

<img src="/static/create_remote_repos.gif" style="border: 1px solid #eee; zoom: 50%;"/>