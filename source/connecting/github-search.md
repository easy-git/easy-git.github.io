# Github搜索存储库，并克隆

easy-git，克隆视图，支持输入`关键字`，在github搜索存储库。

1. 点击`搜索`
2. 输入关键字，比如`vscode`、`jquery`、`vue`等
3. 输入内容后，`回车`，等待返回搜索结果。当存在搜索结果后，会显示出下拉列表；此时点击`url`即可。

<img src="/static/repo_github_search.gif" style="zoom: 50%; border: 1px solid #eee;"/>